#include "ErServer/ErLog.h"
#include <ros/ros.h>

void ErLog::log(LogLevel level, const char *str, ...)
{
//  ROS_INFO(str);
}
 void ErLog::logErrorFromOS(LogLevel level, const char *str, ...)
 {
//   ROS_INFO(str);
 }
